# Отчёт: учёт угловой скорости вращения Земли в AirSim и влияние на данные датасета

**Дата:** 14.08.2026
**Относится к:** CorrectSimulator (v0.6), ModelingSystem-1.1 (v1.1), среда AirSim
**Статус:** информационный документ; код не менялся

---

## Содержание

1. [Постановка вопроса](#1-постановка-вопроса)
2. [Где вращение Земли проявляется в реальных системах](#2-где-вращение-земли-проявляется-в-реальных-системах)
3. [Как устроен AirSim в этом отношении](#3-как-устроен-airsim-в-этом-отношении)
4. [Количественная оценка влияния](#4-количественная-оценка-влияния)
   - 4.1. Гироскоп (угловая скорость)
   - 4.2. Акселерометр (сила Кориолиса)
   - 4.3. GPS и геодезия
   - 4.4. Ground truth и камеры
5. [Вывод: есть ли проблемы с данными](#5-вывод-есть-ли-проблемы-с-данными)
6. [Ограничения и когда это может стать существенным](#6-ограничения-и-когда-это-может-стать-существенным)
7. [Рекомендации](#7-рекомендации)

---

## 1. Постановка вопроса

Вопрос: учитывает ли AirSim (и его сцены на базе Unreal Engine) угловую скорость вращения Земли `Ω ≈ 7.2921159·10⁻⁵ рад/с` (≈ 15.04°/ч), и могут ли из-за отсутствия такого учёта возникнуть проблемы с данными датасета.

Краткий ответ: **нет, вращение Земли в симуляции не моделируется**, но **проблем с данными это не создаёт** — данные внутренне согласованы, а вклад вращения Земли на порядки ниже уровня шумов датчиков. Подробное обоснование ниже.

---

## 2. Где вращение Земли проявляется в реальных системах

В реальной беспилотной системе вращение Земли влияет на:

- **Гироскоп**: MEMS/лазерный гироскоп измеряет абсолютную угловую скорость корпуса **относительно инерциального пространства** (ECI), включая составляющую `Ω`. Именно на этом эффекте основано гирокомпасирование и определение азимута по горизонтальной составляющей `Ω·cos φ` (≈ 11.9°/ч на широте 52°).
- **Акселерометр**: на движущийся аппарат действует кориолисово ускорение `a_C = 2·Ω×v` и центробежное ускорение (учтено в локальном отвесе).
- **Навигационные вычисления**: переносные и кориолисовы члены в уравнениях инерциальной навигации, связь NED ↔ ECEF ↔ ECI.

---

## 3. Как устроен AirSim в этом отношении

AirSim использует:

- **Физику дрона** — упрощённую модель (SimpleFlight или PhysX), работающую в локальной СК **NED** без модели Земли как вращающегося эллипсоида. Кориолисовы, центробежные и переносные ускорения от вращения Земли в уравнения движения **не добавляются**.
- **Синтез IMU** — «идеальные» значения от физики + аддитивные шумы и смещения (параметры из `settings.json`). В гироскоп входит только угловая скорость дрона в NED, член `Ω` **не добавляется**.
- **GPS** — симуляция из истинной позиции NED через геодезические координаты (`HomePoint`, WGS-84) с аддитивным шумом. Преобразование геодезических координат в локальную СК выполняется через ECEF, т.е. без ошибок «плоской земли».
- **Сцены Unreal Engine** — статичные миры: вращение Земли, движение небесной сферы, изменения освещённости за счёт суточного вращения **не моделируются**.

Важно: физика, IMU и ground truth работают в **одной и той же** невращающейся локальной СК. Это значит, что датчики и эталон согласованы между собой — не возникает «рассогласования», которое появлялось бы, если бы физика игнорировала Ω, а GT строился на ECEF.

---

## 4. Количественная оценка влияния

Сравнение с параметрами датчиков из `settings.json` CorrectSimulator.

### 4.1. Гироскоп (угловая скорость)

| Величина | Значение |
|---|---|
| Скорость вращения Земли `Ω` | 7.292·10⁻⁵ рад/с ≈ 0.00418 °/с ≈ 15.04 °/ч |
| `GyroNoiseDensity` (настройка) | 3.394·10⁻⁴ рад/с/√Гц |
| СКО шума за полосу ~100 Гц (200 отсчётов/с) | ≈ 3.4·10⁻³ рад/с ≈ 0.19 °/с |
| `GyroBiasStability` (настройка) | 1·10⁻⁴ рад/с ≈ 0.0057 °/с |

Вывод: `Ω` (0.00418 °/с) примерно в **45 раз ниже** СКО шума гироскопа и на уровне нестабильности смещения. На полётах длительностью в минуты влияние вращения Земли на оценку ориентации ниже уровня шума.

### 4.2. Акселерометр (сила Кориолиса)

`a_C = 2·Ω·v`. При скорости дрона до 20 м/с:

| Величина | Значение |
|---|---|
| `a_C` (2·7.292·10⁻⁵·20) | ≈ 2.9·10⁻³ м/с² ≈ 0.3 mg |
| `AccelNoiseDensity` (настройка) | 4·10⁻³ м/с²/√Гц |
| СКО шума за полосу ~100 Гц | ≈ 4·10⁻² м/с² ≈ 4 mg |

Вывод: кориолисово ускорение (~0.3 mg) примерно в **13 раз ниже** СКО шума акселерометра — неразличимо на фоне шума.

### 4.3. GPS и геодезия

Переход геодезических координат в локальную NED выполняется через ECEF/WGS-84 — это точное преобразование, ошибок «плоской земли» на масштабах сценария (единицы километров) не даёт. Единственный источник ошибки позиции — заданный шум GPS, который не зависит от учёта вращения Земли.

### 4.4. Ground truth и камеры

- GT ориентации/позиции/скоростей берётся из той же физики NED — эталон согласован с датчиками, вращение Земли в него не входит и не должно входить (иначе возникла бы несогласованность).
- Камеры фиксируют статичную сцену; отсутствие вращения Земли не влияет на корректность кадров и таймстампов.

---

## 5. Вывод: есть ли проблемы с данными

**Практически нет.**

1. Вклад вращения Земли (гироскоп ~0.0042 °/с, кориолисово ускорение ~0.3 mg) на 1–2 порядка ниже шумов датчиков — на данных он неразличим.
2. Данные **внутренне согласованы**: физика, IMU и GT находятся в одной невращающейся СК NED. Отсутствие вращения Земли не создаёт расхождений между «эталоном» и «показаниями датчиков».
3. Для типовых потребителей датасета (VIO, SLAM, оценка ориентации фильтром Калмана, сравнение с GT) вращение Земли не требуется.

---

## 6. Ограничения и когда это может стать существенным

Существенным отсутствие учёта вращения Земли становится только для задач, которые **опираются на наблюдаемость этого эффекта**:

- **Прецизионная инерциальная навигация (INS)** класса навигационных систем, полёты длительностью в часы — накопление пропущенного члена `Ω` даст дрейф в оценке курса.
- **Гирокомпасирование и точное выставление азимута** по горизонтальной составляющей `Ω·cos φ` — в данных этого сигнала просто нет, такой алгоритм не сойдётся.
- **Тестирование алгоритмов, в которых гироскоп трактуется как измеряющий скорость относительно инерциального пространства** с последующей компенсацией `Ω` — компенсация добавит фиктивную поправку, т.к. фактически гироскоп измеряет скорость относительно NED.

Во всех этих случаях речь идёт об **ограничении модели**, а не об ошибке данных: датасет честно отражает ту модель, которая в нём зашита.

---

## 7. Рекомендации

1. Зафиксировать в README/отчёте допущение: «симуляция выполнена в локальной невращающейся СК NED; вращение Земли не моделируется; датчики и GT согласованы между собой».
2. Для задач, не требующих вращения Земли, ничего менять не нужно.
3. Если позже потребуется проверять прецизионную INS/гирокомпасирование, корректнее всего добавить `Ω` **на этапе постобработки** IMU, а не переделывать симуляцию:
   - в показания гироскопа добавить `ω_b = C_n^b · ω_ie^n`, где `ω_ie^n = [Ω·cos φ, 0, −Ω·sin φ]` в СК NED;
   - при желании — кориолисов член `−2·(Ω_ie^n × v^n)` в уравнения акселерометра;
   - GT при этом остаётся в NED без изменений.

Такой подход сохраняет совместимость с текущей структурой датасета и позволяет «включать» вращение Земли только там, где оно действительно нужно.
