# Проблема с кватернионами в ModelingSystem-1.1 и её исправление в CorrectSimulator

Подробный разбор: что такое кватернион, как обязана выглядеть смена системы координат (СК) для ориентации, почему применённое в ModelingSystem-1.1 преобразование `(w, x, −z, y)` даёт неверные результаты, и как это исправлено в CorrectSimulator.

---

## Содержание

1. [Кватернион ориентации](#1-кватернион-ориентации)
2. [Две роли кватерниона: оператор поворота и ориентация](#2-две-роли-кватерниона-оператор-поворота-и-ориентация)
3. [Смена СК для векторов](#3-смена-ск-для-векторов)
4. [Смена СК для оператора поворота — сопряжение](#4-смена-ск-для-оператора-поворота--сопряжение)
5. [Смена СК для ориентации — левое умножение (вывод)](#5-смена-ск-для-ориентации--левое-умножение-вывод)
6. [Что делал ModelingSystem-1.1](#6-что-делал-modelingsystem-11)
7. [Почему это неправильно](#7-почему-это-неправильно)
   - 7.1. Перестановка = сопряжение `q_R` (это правило для оператора)
   - 7.2. Числовой контрпример: yaw ↔ pitch
   - 7.3. Когда ошибочное правило «случайно» совпадает с верным
8. [Как исправлено в CorrectSimulator](#8-как-исправлено-в-correctsimulator)
9. [Последствия и выводы](#9-последствия-и-выводы)

---

## 1. Кватернион ориентации

Кватернион `q = (w, x, y, z)` — запись поворота через ось и угол:

```
q = (cos θ/2,  sin θ/2 · n),   |q| = 1
```

где `n = (n_x, n_y, n_z)` — единичный вектор оси поворота, `θ` — угол. Единичный кватернион поворачивает вектор `v` двусторонним умножением (сопряжением):

```
v' = q ⊗ v ⊗ q⁻¹            (q⁻¹ = (w, −x, −y, −z) для единичного q)
```

Важные свойства:
- кватернионы — это **не** 3D-векторы: четыре компоненты нелинейно параметризуют вращение в SO(3);
- `q` и `−q` задают один и тот же поворот (двойное накрытие);
- произведение единичных кватернионов — снова единичный кватернион (норма сохраняется).

---

## 2. Две роли кватерниона: оператор поворота и ориентация

Один и тот же математический объект используется в двух разных смыслах:

**а) Оператор поворота.** Кватернион задаёт преобразование векторов `v → q ⊗ v ⊗ q⁻¹`. Это «сам поворот» — отвлечённо от базиса.

**б) Ориентация (поза).** Кватернион связывает две СК: как корпус дрона повёрнут относительно мировой СК. Для любого вектора `v` в корпусе координаты в мире равны

```
w_NED = q_NED ⊗ v ⊗ q_NED⁻¹
```

Именно ориентация записывается в ground truth датасета.

Различие критично: **правило смены СК для этих двух ролей разное**. ModelingSystem-1.1 применил правило для оператора поворота там, где нужна ориентация, — отсюда ошибка.

---

## 3. Смена СК для векторов

Смена мировой СК NED → F (X=север, Y=вверх, Z=восток) — это поворот осей на −90° вокруг оси X. В матричном виде:

```
M  (F → NED):  (x, y, z) → (x,  z, −y)
M⁻¹ (NED → F): (x, y, z) → (x, −z,  y)
```

Для **векторов** (позиция, скорость, ускорение) переход — умножение на `M⁻¹`, которое здесь сводится к перестановке `(x, −z, y)`. Поэтому перестановка для векторов **корректна** — и именно поэтому возник соблазн применить нечто похожее к кватерниону.

---

## 4. Смена СК для оператора поворота — сопряжение

Пусть `R` — оператор поворота в старом базисе. В новом базисе (матрица перехода `M`) оператор преобразуется как `R' = M⁻¹ R M`. На уровне кватернионов этому соответствует **сопряжение**:

```
q' = q₀ ⊗ q ⊗ q₀⁻¹,   где q₀ — кватернион перехода
```

Это правило преобразует *оператор поворота* при смене базиса.

---

## 5. Смена СК для ориентации — левое умножение (вывод)

Пусть ориентация задана кватернионом `q_NED` (корпус → NED):

```
w_NED = q_NED ⊗ v ⊗ q_NED⁻¹
```

Хотим `q_F` такой, что в СК F тот же вектор наблюдается как `w_F = q_F ⊗ v ⊗ q_F⁻¹`. Связь между СК: `w_F = M⁻¹ w_NED`. Подставляем:

```
w_F = M⁻¹ (q_NED ⊗ v ⊗ q_NED⁻¹)
    = q_{M⁻¹} ⊗ (q_NED ⊗ v ⊗ q_NED⁻¹) ⊗ q_{M⁻¹}⁻¹
    = (q_{M⁻¹} ⊗ q_NED) ⊗ v ⊗ (q_{M⁻¹} ⊗ q_NED)⁻¹
⇒  q_F = q_{M⁻¹} ⊗ q_NED
```

где `q_{M⁻¹}` — кватернион поворота `M⁻¹` (NED → F). Для нашей пары СК:

```
q_M   = (cos(−45°), sin(−45°), 0, 0) = (1/√2, −1/√2, 0, 0)   # F → NED
q_{M⁻¹} = (1/√2, 1/√2, 0, 0) = q_R
```

Итоговое правило — **левое умножение**:

```
q_F = q_R ⊗ q_NED,   q_R = (1/√2, 1/√2, 0, 0)
```

Сравните с сопряжением из раздела 4: там `q_R` умножается **с обеих сторон** (`q_R ⊗ q ⊗ q_R⁻¹`), здесь — **только слева** (`q_R ⊗ q`). Это разные операции, и лишь вторая верна для ориентации.

---

## 6. Что делал ModelingSystem-1.1

В `AirSimDroneConnector.get_ground_truth()` и `SensorRecorder.record_ground_truth()` / `record_ground_estimate()` компоненты кватерниона механически переставлялись:

```python
qw, qx, qy, qz = orient.w_val, orient.x_val, -orient.z_val, orient.y_val
```

т.е. применялось отображение `(w, x, y, z) → (w, x, −z, y)`.

---

## 7. Почему это неправильно

### 7.1. Перестановка = сопряжение `q_R` (это правило для оператора)

Покажем, что отображение `(w, x, y, z) → (w, x, −z, y)` — это ровно сопряжение кватернионом `q_R = (1/√2, 1/√2, 0, 0)`:

```
q_R ⊗ q ⊗ q_R⁻¹ = (w, x, −z, y)
```

(тождество проверяется прямым перемножением; оно выполняется для произвольного единичного `q`).

То есть ModelingSystem применил **правило для оператора поворота** (раздел 4) — сопряжение — к величине, которая является **ориентацией** (для неё верно левое умножение, раздел 5). Сопряжение «доворачивает ось поворота» вокруг оси X на 90°, но не выполняет переход ориентации в новую СК.

### 7.2. Числовой контрпример: yaw ↔ pitch

**Пример 1: чистый yaw 90° (нос на восток).** В NED это поворот вокруг оси Z:

```
q_NED = (0.707, 0, 0, −0.707)
```

- ModelingSystem: `(w, x, −z, y) = (0.707, 0, 0.707, 0)` — это **чистый pitch вверх**, а не yaw. Дрон, летящий носом на восток, записан как «задравший нос».
- CorrectSimulator: `q_F = q_R ⊗ q_NED = (0.5, 0.5, 0.5, −0.5)` — проверка: применение к вектору «вперёд» даёт ось восток. Верно.

**Пример 2: чистый pitch 90° (нос вверх).** В NED:

```
q_NED = (0.707, 0, 0.707, 0)
```

- ModelingSystem: `(0.707, 0, 0, 0.707)` — это **yaw 90°**, а не pitch. «Нос вверх» записан как «нос на восток».
- CorrectSimulator: `(0.5, 0.5, 0.5, 0.5)` — проверка: «вперёд» → вверх, «живот» → север. Верно.

Вывод: ошибочное правило **меняет местами yaw и pitch** (и вообще произвольно искажает ось/угол), т.е. в датасете ориентация физически не соответствует полёту.

### 7.3. Когда ошибочное правило «случайно» совпадает с верным

`q_F = q_R ⊗ q` и `q_F = q_R ⊗ q ⊗ q_R⁻¹` совпадают тогда и только тогда, когда `q` коммутирует с `q_R`, т.е. когда поворот идёт вокруг той же оси X (ось «вперёд»). Это **чистый крен (roll)**. Для крена перестановка даёт верный результат — поэтому ошибка могла оставаться незаметной при тестировании только на крене, но ломала pitch и yaw.

---

## 8. Как исправлено в CorrectSimulator

В `SensorRecorder.py`:

```python
@staticmethod
def _quat_to_drone_frame(q):
    s = 0.7071067811865476
    w, x, y, z = q.w_val, q.x_val, q.y_val, q.z_val
    return s * (w - x), s * (x + w), s * (y - z), s * (z + y)
```

Это ровно левое умножение `q_F = q_R ⊗ q_NED` (развернутое покомпонентно):

```
q_R ⊗ (w, x, y, z) = s·(w−x), s·(x+w), s·(y−z), s·(z+y)
```

Дополнительно:
- углы Эйлера считаются **от сырого NED-кватерниона** (`_quat_to_euler`), где roll/pitch привязаны к горизонту, а yaw — к северу (в записи против часовой стрелки: 0°=север, +90°=запад); в СК F их пересчитывать не требуется;
- всё преобразование локализовано в одном модуле, что исключает повторение ошибки в другом месте.

---

## 9. Последствия и выводы

Последствия ошибки в ModelingSystem-1.1:
- кватернионы в `state_ground_truth0` искажены — истинная ориентация в датасете потеряна;
- roll/pitch/yaw, посчитанные от переставленного кватерниона, физически неверны (не привязаны ни к горизонту, ни к северу);
- любой потребитель (оценщик ориентации, сравнение с IMU, анализ манёвров) получает некорректный эталон, а искажения не являются постоянным смещением — они зависят от текущей ориентации.

Главный методический вывод: **нельзя преобразовывать кватернион «теми же перестановками, что и векторы»**. Смена СК для вектора — матричное умножение; для оператора поворота — сопряжение; для ориентации — левое умножение на кватернион перехода. Только последнее корректно в ground truth датасета.
